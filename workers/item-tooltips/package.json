{
  "name": "item-tooltip-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc -p test/tsconfig.json --noEmit",
    "types": "wrangler types",
    "dry-run": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.8",
    "typescript": "5.9.3",
    "vitest": "4.1.11",
    "wrangler": "4.131.1"
  }
}
